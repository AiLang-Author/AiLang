// test_dynamic_pool.ailang
// Test DynamicPool compilation fix

PrintMessage("Testing DynamicPool")
PrintMessage("===================")

// This should now compile without error
DynamicPool.TestDynamic {
    "counter": Initialize=100
    "buffer": Initialize=0
    "size": Initialize=256
}

PrintMessage("DynamicPool declared successfully!")

// Test reading initial values
PrintMessage("Initial counter (should be 100):")
PrintNumber(TestDynamic.counter)
PrintMessage("Initial buffer (should be 0):")
PrintNumber(TestDynamic.buffer)
PrintMessage("Initial size (should be 256):")
PrintNumber(TestDynamic.size)

// Test writing to DynamicPool
TestDynamic.counter = Add(TestDynamic.counter, 1)
TestDynamic.buffer = 0xDEADBEEF
Te