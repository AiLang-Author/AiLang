// test_connect_primitive.ailang
sock = SocketCreate()
PrintMessage("Socket: ")
PrintNumber(sock)

// Connect to Redis (we know it's running on 127.0.0.1:6379)
ip = 16777343  // 127.0.0.1
result = SocketConnect(sock, ip, 6379)

PrintMessage("Connect result: ")
PrintNumber(result)