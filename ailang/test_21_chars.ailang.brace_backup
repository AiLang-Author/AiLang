PrintMessage("Testing 21 character name")

Function.ABCD.EFGHIJKLMNOPQ {
    Body: {
        PrintMessage("21 chars total - should crash?")
        ReturnValue(21)
    }
}

PrintMessage("About to call 21 char function...")
result = ABCD.EFGHIJKLMNOPQ()
PrintMessage("If you see this, it worked")
PrintNumber(result)
