PrintMessage("Testing array parameter")

Function.Test.ProcessArray {
    Input: arr:
    Body: {
        PrintMessage("Inside ProcessArray")
        first = ArrayGet(arr, 0)
        PrintNumber(first)
        ReturnValue(first)
    }
}

PrintMessage("Creating array...")
my_array = ArrayCreate(3)
ArraySet(my_array, 0, 42)

PrintMessage("Passing array to function...")
result = Test.ProcessArray(my_array)
PrintMessage("Done!")
