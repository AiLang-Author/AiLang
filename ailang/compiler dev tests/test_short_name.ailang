PrintMessage("Test short name")

Function.RESP.Make {
    Body: {
        PrintMessage("Inside RESP.Make")
        ReturnValue(42)
    }
}

PrintMessage("Calling RESP.Make")
result = RESP.Make()
PrintMessage("Function returned")
PrintNumber(result)
