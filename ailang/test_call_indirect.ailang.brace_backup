// test_call_indirect.ailang
Function.AddTen {
    Input: x: Integer
    Output: Integer
    Body: {
        result = Add(x, 10)
        ReturnValue(result)
    }
}

ptr = AddressOf(AddTen)
result = CallIndirect(ptr, 5)
PrintNumber(result)  // Should print 15