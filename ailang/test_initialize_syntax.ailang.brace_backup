// test_initialize_syntax.ailang
// Regression test for Initialize= syntax

PrintMessage("Initialize Syntax Test")
PrintMessage("======================")

// Test with various values
FixedPool.InitTest {
    "zero": Initialize=0
    "positive": Initialize=100
    "large": Initialize=65536
}

PrintMessage("zero (should be 0):")
PrintNumber(InitTest.zero)
PrintMessage("positive (should be 100):")
PrintNumber(InitTest.positive)
PrintMessage("large (should be 65536):")
PrintNumber(InitTest.large)

PrintMessage("Initialize syntax test passed!")