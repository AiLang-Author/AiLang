// test_pool_minimal.ailang
// Minimal test to isolate the segfault

PrintMessage("Starting minimal pool test")

// Test just one FixedPool with one variable
FixedPool.Simple {
    "value": Initialize=42
}

PrintMessage("Pool declared")
PrintMessage("Value should be 42:")
PrintNumber(Simple.value)

PrintMessage("Test complete")