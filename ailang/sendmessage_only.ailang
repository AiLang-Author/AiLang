// sendmessage_only.ailang
// Test just SendMessage

PrintMessage("Testing SendMessage...")

SubRoutine.TestSend {
    PrintMessage("Before send")
    
    // According to parse_sendmessage, syntax is:
    // SendMessage.target(param-value, param2-value)
    // Or just SendMessage.target for no parameters
    SendMessage.TestChannel
    
    PrintMessage("After send")
}

RunTask(TestSend)

PrintMessage("Test complete")