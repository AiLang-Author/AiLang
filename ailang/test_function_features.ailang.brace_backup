// test_function_features.ailang
// Test various function features

PrintMessage("Function Features Test")
PrintMessage("======================")

// Test function with no params
Function.Utils.GetConstant {
    Body: {
        ReturnValue(999)
    }
}

// Test function with single param
Function.Utils.Double {
    Input: n:
    Body: {
        result = Multiply(n, 2)
        ReturnValue(result)
    }
}

// Test function with multiple params (proper syntax)
Function.Utils.AddThree {
    Input: a:
    Input: b:
    Input: c:
    Body: {
        sum1 = Add(a, b)
        sum2 = Add(sum1, c)
        ReturnValue(sum2)
    }
}

// Test the functions
const = Utils.GetConstant()
PrintMessage("Constant (999):")
PrintNumber(const)

doubled = Utils.Double(21)
PrintMessage("Double(21) = 42:")
PrintNumber(doubled)

sum3 = Utils.AddThree(10, 20, 30)
PrintMessage("AddThree(10,20,30) = 60:")
PrintNumber(sum3)

PrintMessage("Function features test complete!")